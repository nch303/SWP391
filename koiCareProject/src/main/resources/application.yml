server:
  port: 8080
  servlet:
    context-path: /sunsidekoi

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/koisystem"
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true